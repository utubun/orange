import React from "react";
import './box.css';

export const Box = ({theme}) => (
    <div className={`bp4-card ${theme}`}>
        <div className="box">
            <h1>There is nothing to show</h1>
        </div>
    </div>
);